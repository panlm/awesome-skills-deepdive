# fzf-fuzzy-finder

> 使用 fzf 进行模糊搜索和文件查找

## 基本信息

| 项目 | 内容 |
|---|---|
| **名称** | fzf-fuzzy-finder |
| **作者** | arnarsson |
| **ClawHub** | https://clawskills.sh/skills/arnarsson-fzf-fuzzy-finder |
| **GitHub** | https://github.com/openclaw/skills/tree/main/skills/arnarsson/fzf-fuzzy-finder |
| **安全评级** | 🟢 ClawHub Verified (Benign) |

## 功能概述

- Use `--preview` for visual context
- Combine with `bat`, `rg`, `fd` for powerful workflows
- Press `?` in fzf to see keybindings
- Use `Tab` for multi-select
- Start query with `'` for exact match
- Start with `!` to exclude
- Use `|` for OR, space for AND
- Set `FZF_DEFAULT_OPTS` for persistent config

## 使用场景

fzf is a general-purpose command-line fuzzy finder that filters any piped input interactively. It integrates with shell keybindings, git, vim, docker, and kubectl through standard Unix pipes.

## 依赖和前提条件

- 无特殊依赖

## 安全状态

| 来源 | 评级 |
|---|---|
| VirusTotal | 🟢 Benign |
| OpenClaw | 🟢 Benign |

> ClawHub 安全扫描已通过，跳过详细审计。

---

> 本文档由 awesome-skills-deepdive skill 自动生成，仅供参考。
> 生成时间: 2026-04-01 04:44 UTC
